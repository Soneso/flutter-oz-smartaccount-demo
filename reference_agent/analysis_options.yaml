# Static analysis for the reference_agent package.
#
# Extends the Flutter recommended lint set with the stricter rules used across
# the demo source. Zero warnings is the baseline — any unresolved issue blocks
# the build.
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    avoid_dynamic_calls: error
    unused_import: error
    unused_local_variable: error

linter:
  rules:
    # --- Type safety ---
    avoid_dynamic_calls: true

    # --- Immutability ---
    prefer_final_fields: true
    prefer_final_locals: true

    # --- Dart style best-practices (consistent with the demo base) ---
    always_declare_return_types: true
    annotate_overrides: true
    avoid_empty_else: true
    avoid_redundant_argument_values: true
    avoid_relative_lib_imports: true
    await_only_futures: true
    directives_ordering: true
    exhaustive_cases: true
    noop_primitive_operations: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_if_null_operators: true
    prefer_single_quotes: true
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_null_checks: true
    use_named_constants: true
