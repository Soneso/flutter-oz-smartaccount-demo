/// Shared spacing and radius tokens used across screens and widgets.
library;

import 'package:flutter/widgets.dart';

const EdgeInsets kCardPadding = EdgeInsets.all(16);
const EdgeInsets kInsetPadding = EdgeInsets.all(12);
const EdgeInsets kSummaryCardPadding = EdgeInsets.all(14);

const Radius kCardRadius = Radius.circular(10);
const Radius kChipRadius = Radius.circular(8);
const Radius kButtonRadius = Radius.circular(12);

const int kDisabledStateAlpha = 180;
const int kErrorBorderAlpha = 76;
const int kSecondaryEmphasisAlpha = 200;
